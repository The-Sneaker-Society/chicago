/**
 * SCAFFOLD — teaching example, not wired to any resolver yet.
 */

export const groupPostErrors = Object.freeze({
  notFound: "POST_NOT_FOUND",
  groupNotFound: "GROUP_NOT_FOUND",
  forbidden: "FORBIDDEN",
  contentRequired: "POST_CONTENT_REQUIRED",
});
